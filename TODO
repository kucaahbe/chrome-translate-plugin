current:
* If you're using message passing, be sure to close unused message ports. The event page will not shut down until all message ports are closed.

for future:
add menu item for single translation invocation
